package org.aksw.simba.squirrel.e2e;

import org.junit.Test;

/**
 * @author Ivan Ermilov (iermilov@informatik.uni-leipzig.de)
 */
public class CrawlingWorkflow {

    /**
     * 
     **/
    @Test
    public void testCrawling() {
        // Create SeedURIGenerator
        // get URI from build in string
        // SeedGeneratorZeroMQImpl seedGenerator = new
        // SeedGeneratorZeroMQImpl();
        // CrawleableUri[] seed = seedGenerator.getStaticSeed();
        //
        // System.out.println(Arrays.toString(seed));
        // Create Frontier and populate it with URIs from SeedURIGenerator
        // FrontierImpl frontier = new FrontierImpl();

        // Start 1 worker and start polling for URIs

        // Worker process URI and save crawl data to Sink

        // Sink just writes append everything to one big file

        // Jena loads the file and count triples

        // If triples more than 1 -- success!
        throw new NullPointerException();
    }
}
